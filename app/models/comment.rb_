class Comment < ApplicationRecord
  
  belongs_to :comentable, polymorphic:true #before only posts

	validates :commenter, :presence => true
  validates :body, :presence => true, :length => {:in => 10..255}

end
